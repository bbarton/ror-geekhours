<% if flash[:notice].present? %>
$(".flashnotice").replaceWith('<div class="flashnotice"><h5><%= escape_javascript(flash[:notice]) %></h5></div>');
$(".flashnotice").show(300);
<% else %>
$('#registerForm').html("<%= escape_javascript(render :partial => 'thank_you') %>");
<% end %>